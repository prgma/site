import * as React from 'react';
import styled from 'styled-components';

interface IProps {
  data: any;
  headers: string[];
}

const Table = ({ data, headers }: IProps) => null;

export default Table;
